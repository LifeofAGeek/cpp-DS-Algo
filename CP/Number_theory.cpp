#include <bits/stdc++.h>
using namespace std;

//primality test is to determine whether the input integer is a prime num or not.
bool primality_test(int n)
{
    /* brute force approach is O(n) i.e.,
    if(n==1) return false;
    for (int i = 2; i < n; i++)
    {
        if(n%i==0) return false;
    }
    return true; */

    /* better approach -> all divisor of a number occurs in a pair of 
    
    
    */
}

int main()
{
    cout<<primality_test()<<endl;
}